# Panel (Prisma scaffold DB) — adjust path for your machine
DATABASE_URL=file:./db/custom.db

# Optional overrides (defaults match the reference deployment)
# GLM_PROJECT_ROOT=/path/to/glm-panel/frontend
# GLM_BRIDGE_URL=http://localhost:3001
